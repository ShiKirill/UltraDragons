import styled from "@emotion/styled";

export const StyledSvg = styled("svg")({
  position: "absolute",
  pointerEvents: "none",
  overflow: "visible",
  width: "fit-content",
  height: "fit-content",
});
